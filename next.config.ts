import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['makhi-coldturkey-brady.ngrok-free.dev'],
  devIndicators: false,
};

export default nextConfig;